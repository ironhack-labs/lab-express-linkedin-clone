<!DOCTYPE html>
<html>
  <head>
    <title>Basic Auth</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>

<h1>You are viewing <%= name.toUpperCase() %>'s public profile</h1>
<ul>
  <li>Name: <%= name.toUpperCase() %> </li>

  <% if( image === undefined || image === null) { %>
    <li>Image: <%= name.toUpperCase() %> hasn't uploaded one yet </li>
<%  } else { %>
    <li>Image: <%= image %> </li>
<% } %>

  <% if( job === undefined || job === null) { %>
    <li>Job: <%= name.toUpperCase() %> hasn't filled this out yet </li>
  <%  } else { %>
    <li>Job: <%= job %> </li>
  <% } %>

  <% if( company === undefined || company === null) { %>
    <li>Company: <%= name.toUpperCase() %> hasn't filled this out yet </li>
  <%  } else { %>
    <li>Company: <%= company %> </li>
  <% } %>



</ul>

<h4>If you want to see more information on <%= name.toUpperCase() %>, <a href="/login">click here to log in</a></h4>
<h4>Or if you're new to LinkedIn, <a href="/signup">click here to sign up</a></h4>

</body>
</html>
